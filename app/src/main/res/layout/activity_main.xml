<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.dnsoftindia.soapdemo.MainActivity">

    <EditText
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="10dp"
        android:layout_toStartOf="@+id/btnConvert"
        android:layout_toLeftOf="@+id/btnConvert"
        android:id="@+id/etConvert"
        android:textSize="18sp"
        tools:text="Hello, World!"
        android:hint="@string/enter_message_to_convert"/>

    <Button
        android:layout_width="wrap_content"
        android:layout_height="50dp"
        android:layout_marginEnd="10dp"
        android:layout_marginRight="10dp"
        android:id="@+id/btnConvert"
        android:text="@string/convert"
        android:textStyle="normal"
        android:layout_alignParentEnd="true"
        android:layout_alignParentRight="true"/>

    <ImageView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_margin="10dp"
        android:layout_below="@+id/etConvert"
        android:id="@+id/ivBraille"
        android:contentDescription="@string/app_name"
        android:scaleType="centerInside"/>

</RelativeLayout>
